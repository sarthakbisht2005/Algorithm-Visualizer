export function floydWarshall(grid, start, end) {
    // Not practical for grid, but stub for visualization
    // Returns empty path and visitedOrder for now
    return { visitedOrder: [], path: [] };
}
